/**
 * HTTPリクエストを送り、結果をHTTPレスポンスとして受け取る。
 */

class HttpCommunicationController {
	/**
	 * @param {str} parameter - リクエストパラメータ
	 * @param {str} methodName - リクエストを送るメソッド名
	 * @return {json} 取得したjsonを返す
	 */
	communicateGetProcess(parameter, methodName) {}
}
